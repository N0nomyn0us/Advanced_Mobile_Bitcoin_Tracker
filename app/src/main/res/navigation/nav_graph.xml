<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_login">

    <!-- LOGIN FRAGMENT -->
    <fragment
        android:id="@+id/navigation_login"
        android:name="com.example.advancedmobilebitcointracker.LoginFragment"
        android:label="Login"
        tools:layout="@layout/fragment_login">
        <action
            android:id="@+id/action_login_to_home"
            app:destination="@id/navigation_home"
            app:popUpTo="@id/navigation_login"
            app:popUpToInclusive="true" />
        <action
            android:id="@+id/action_login_to_register"
            app:destination="@id/navigation_register" />
    </fragment>

    <!-- REGISTER FRAGMENT -->
    <fragment
        android:id="@+id/navigation_register"
        android:name="com.example.advancedmobilebitcointracker.RegisterFragment"
        android:label="Register"
        tools:layout="@layout/fragment_register">
        <action
            android:id="@+id/action_register_to_home"
            app:destination="@id/navigation_home"
            app:popUpTo="@id/navigation_login"
            app:popUpToInclusive="true" />
    </fragment>

    <!-- MAIN APP FRAGMENTS -->
    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.advancedmobilebitcointracker.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/navigation_portfolio"
        android:name="com.example.advancedmobilebitcointracker.PortfolioFragment"
        android:label="Portfolio"
        tools:layout="@layout/fragment_portfolio" />

    <fragment
        android:id="@+id/navigation_search"
        android:name="com.example.advancedmobilebitcointracker.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.example.advancedmobilebitcointracker.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings">

        <!-- NEW LOGOUT ACTION: Clears history so user can't go back -->
        <action
            android:id="@+id/action_settings_to_login"
            app:destination="@id/navigation_login"
            app:popUpTo="@id/navigation_home"
            app:popUpToInclusive="true" />
    </fragment>
</navigation>